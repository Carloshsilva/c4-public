No fluxo do ProcessingService.processRequestAsynchronously (ou onde o processamento acontece), qual a ordem das etapas entre receber a requisição e chamar a SELIC? Especificamente: onde está a validação de payload/token, e o que vem depois dela (montagem de JSON, checagem de duplicação, etc.) até a chamada sendMessageToSelic? Quero identificar o ponto logo APÓS a validação de payload e ANTES do processamento pesado.
